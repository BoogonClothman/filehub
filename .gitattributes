# Force LF for shell scripts and systemd units (deployed to Linux)
*.sh text eol=lf
*.service text eol=lf

# Force CRLF for Windows batch files
*.bat text eol=crlf

# Go files: LF is fine everywhere
*.go text eol=lf
